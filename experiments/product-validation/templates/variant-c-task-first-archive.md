# Variant C — Task-first Archive

Before starting the same existing CLI, participant completes only `minimal-task-card.md`. The card may not be expanded with productized fields.

After execution, user-visible archive fields and presentation must be exactly the same as Variant B. The only intended independent variable is the pre-start Task contract.

Record whether Task facts were actually reused during execution, Verification, recovery, or Review; mere completion of the card is not benefit evidence.
