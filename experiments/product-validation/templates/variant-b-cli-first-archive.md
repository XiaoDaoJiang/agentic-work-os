# Variant B — CLI-first Archive

Participant still starts through the existing CLI. Archive population is performed by script or Wizard-of-Oz without additional user entry/cleanup work.

The participant-visible archive must use exactly `common-archive-contract.yaml`. Its fields, presentation, runtime/model/configuration, working-copy policy, facilitator help and Review material must match Variant C.

Any visible extra collection burden or B/C presentation difference makes the comparison confounded.
